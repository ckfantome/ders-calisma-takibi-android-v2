<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <!-- Arkaplan takibinin OEM pil yoneticileri tarafindan oldurulmesini
         azaltmak icin kullaniciya istisna istemi gosterebilmek amaciyla. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Otomatik Baslatma: cihaz yeniden baslayinca arkaplan servisini sessizce
         ayaga kaldirabilmek icin (bkz. service/BootAndRestartReceiver.kt). -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Gunluk yedekleme e-postasinin SMTP ile gonderilebilmesi icin. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- GitHub uzerinden guncelleme: indirilen APK'yi kurabilmek icin. Normal
         izin dialoguyla ISTENEMEZ, kullanici Ayarlar > Bilinmeyen kaynaklar'dan
         acikca vermeli (bkz. util/UpdateInstaller.kt). -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- Uygulama Kullanimi ekrani: normal izin dialoguyla ISTENEMEZ, kullanici
         Ayarlar > Ozel erisim > Kullanim erisimi'nden acikca vermeli. -->
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />
    <!-- Ebeveyn-denetim: arama/mesaj ozeti, konum/guvenli bolge (normal runtime
         izin dialoguyla istenir). Bildirim erisimi (asagida) ozel bir izindir. -->
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-feature android:name="android.hardware.camera.front" android:required="false" />
    <!-- READ_SMS/READ_CALL_LOG telefon donanimi (telephony) ima eder - bu
         belirtilmezse Play/ChromeOS uyumluluk taramasi tableti/ChromeOS'u
         desteklenmiyor sayar. required=false: telefon ozelligi yoksa Arama/SMS
         ozeti sessizce calismaz, uygulamanin geri kalani etkilenmez. -->
    <uses-feature android:name="android.hardware.telephony" android:required="false" />

    <!-- Android 11+ paket gorunurlugu kisitlamasi: bu deklarasyon olmadan
         PackageManager.getInstalledApplications() gercek cihazlarda neredeyse
         bos donuyor, bu yuzden Uygulama Kilidi'ndeki "Uygulama Ekle" listesi
         calismıyordu. Sadece baslatilabilir (launcher) uygulamalari gormek
         yeterli, QUERY_ALL_PACKAGES gibi daha genis bir izin gerekmiyor. -->
    <!-- Sinav Modu "her seyi engelle" listesi asla ana ekrani veya telefon/aramayi
         engellememeli (aksi halde cihaz kullanilamaz hale gelir - bkz.
         StudyEngine.essentialSafePackages()). MAIN+LAUNCHER'dan FARKLI intent
         filtreleri oldugu icin (MAIN+HOME, DIAL) HER BIRI AYRI AYRI deklare
         edilmeli - aksi halde Android 11+ paket gorunurlugu bu sorguları da
         bos donduruyor, ana ekran "guvenli" sayilmayip surekli engelleniyordu. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.HOME" />
        </intent>
        <intent>
            <action android:name="android.intent.action.DIAL" />
        </intent>
    </queries>

    <application
        android:name=".StudyTrackerApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.DersCalismaTakibi">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.DersCalismaTakibi">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.StudyForegroundService"
            android:exported="false"
            android:foregroundServiceType="camera" />

        <!-- Otomatik Baslatma (BOOT_COMPLETED) + Surekli Acik Kalma (StudyForegroundService'in
             kendi AlarmManager'iyla tetikledigi ACTION_RESTART_SERVICE) icin ortak alici. -->
        <receiver
            android:name=".service.BootAndRestartReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Cihaz Yoneticisi: kullanici Ayarlar ekranindan acikca aktif eder
             (ACTION_ADD_DEVICE_ADMIN), adb/fabrika sifirlama GEREKMEZ. -->
        <receiver
            android:name=".service.StudyDeviceAdminReceiver"
            android:exported="true"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <meta-data android:name="android.app.device_admin" android:resource="@xml/device_admin" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

        <!-- Bildirim log - ozel izin, kullanici Ayarlar > Bildirim erisimi'nden
             acikca vermeli (Settings.ACTION_NOTIFICATION_LISTENER_SETTINGS). -->
        <service
            android:name=".service.AppNotificationListenerService"
            android:exported="false"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Uygulama Kilidi - ozel izin, kullanici Ayarlar > Erisilebilirlik'ten
             acikca vermeli (Settings.ACTION_ACCESSIBILITY_SETTINGS). -->
        <service
            android:name=".service.AppBlockAccessibilityService"
            android:exported="false"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <activity
            android:name=".BlockedActivity"
            android:exported="false"
            android:excludeFromRecents="true" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
